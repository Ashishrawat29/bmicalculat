{
  "devDependencies": {
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy --config wrangler.toml",
    "preview": "wrangler dev"
  }
}
