{
  "devDependencies": {
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler pages deploy dist --project-name bmicalculat",
    "preview": "wrangler dev"
  }
}
